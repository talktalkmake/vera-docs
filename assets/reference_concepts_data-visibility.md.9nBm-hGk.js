import{_ as t,o,c as d,ag as r}from"./chunks/framework.8tqBsFWt.js";const u=JSON.parse('{"title":"Field-Level Data Visibility","description":"The Field × Role visibility contract — which sensitive fields each role may see, and exactly where in the code that is enforced.","frontmatter":{"description":"The Field × Role visibility contract — which sensitive fields each role may see, and exactly where in the code that is enforced."},"headers":[],"relativePath":"reference/concepts/data-visibility.md","filePath":"reference/concepts/data-visibility.md"}'),a={name:"reference/concepts/data-visibility.md"};function s(i,e,n,c,l,h){return o(),d("div",null,[...e[0]||(e[0]=[r('<h1 id="field-level-data-visibility" tabindex="-1">Field-Level Data Visibility <a class="header-anchor" href="#field-level-data-visibility" aria-label="Permalink to &quot;Field-Level Data Visibility&quot;">​</a></h1><p><code>roles.md</code> answers <strong>Action × Role</strong> — can a Manager create a project, approve time off, cancel a liability. It does not answer a narrower, equally load-bearing question: <strong>which specific fields</strong> (a dollar figure, a rate, a deadline) does a given response actually carry for a given role. Those answers have lived only in scattered prose across CLAUDE.md and in the code itself, which is how #2092 (the manager-visible LER ratio) and #2095 (the manager-accessible person page) shipped without a single edit to <code>roles.md</code>.</p><p><strong>This page is internal-facing.</strong> It names real column names, endpoint names, and payload keys, so it must never be linked from or folded into <code>docs/logic-underneath.md</code> — that page is prospect-facing and must never mention internal vocabulary. This page is not in the VitePress navigation and is not meant to be; like <code>docs/reference/schema/columns.md</code>, it is a working reference for whoever is changing the code.</p><p><strong>This page documents the current boundary. It does not change it.</strong> Every row below was verified against the code as of the date this page was written; if you find a mismatch while reading it, the code is truth and this page is stale — fix the page, and if the mismatch looks like an unintended leak, stop and open a separate issue rather than folding a permission fix into a docs change.</p><p><strong>Enforced by <a href="../../../tests/data-visibility.test.js"><code>tests/data-visibility.test.js</code></a>.</strong> That test parses the table below and checks a live response against it for a curated set of routes — so a field that starts leaking, or a table edit that no longer matches the code, fails <code>npm test</code> rather than waiting to be noticed.</p><hr><h2 id="field-×-role" tabindex="-1">Field × Role <a class="header-anchor" href="#field-×-role" aria-label="Permalink to &quot;Field × Role&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Where it appears</th><th>Owner</th><th>Admin</th><th>Manager</th><th>User</th><th>Note</th></tr></thead><tbody><tr><td>Hourly cost rate (<code>rate_history.hourly_cost</code>)</td><td>Rates tab, Person page, Direct labor tab, cost/P&amp;L calculations</td><td>✅</td><td>✅</td><td>❌</td><td>❌</td><td><code>/console/users</code> (all four tabs, including Rates and Direct labor) is gated <code>isOwnerLevel(user.role)</code> at the top of <code>router.get(&#39;/users&#39;, ...)</code> in <code>lib/console-routes.js</code>. On <code>/console/people/:userId</code>, <code>person.hourly_cost</code> is set only <code>if (isOwner)</code> in that route&#39;s handler — the field is structurally absent from a manager&#39;s payload, not merely hidden — and <code>renderConsolePerson</code> (<code>lib/console/render.js</code>) separately gates the whole Role/Employment type/Discipline/Email/rate <code>&lt;dl&gt;</code> block on <code>isOwnerLevel(user.role)</code> (issue #2095). Double-gated by design; see the note on Mutation 1 below.</td></tr><tr><td><code>contract_value</code></td><td>Project detail Financials card (<code>/console/p/:id</code>), New-project form, Reports, Forecast, Proposals</td><td>✅</td><td>✅</td><td>❌</td><td>❌</td><td><code>vm.contract_value</code> is set only <code>if (isOwnerLevel(user.role))</code> in the <code>/p/:projectId</code> handler (<code>lib/console-routes.js</code>); <code>renderProjectDetail</code> renders the Financials read row only <code>if (project.contract_value !== undefined)</code> (<code>lib/console/render.js</code>) — a single enforcement point (the route), trusted by the renderer. At the MCP layer, <code>get_project_info</code> (<code>mcp-server.js</code>) adds <code>response.contract_value</code> only <code>if (isOwnerLevel(requestingRole))</code>. <code>/console/reports</code> and <code>/console/users</code> are separately whole-route owner-only.</td></tr><tr><td><code>billing_rate</code></td><td>Same surfaces as <code>contract_value</code></td><td>✅</td><td>✅</td><td>❌</td><td>❌</td><td>Same enforcement as <code>contract_value</code>: <code>vm.billing_rate</code> set only <code>if (isOwnerLevel(user.role))</code> in <code>lib/console-routes.js</code>; rendered only <code>if (project.billing_rate !== undefined)</code> in <code>lib/console/render.js</code>; <code>get_project_info</code> (<code>mcp-server.js</code>) gates <code>response.billing_rate</code> the same way.</td></tr><tr><td>LER dollar components (<code>ler_revenue</code> / <code>ler_cost</code>)</td><td>Monthly P&amp;L, PM comparison rollups</td><td>✅</td><td>✅</td><td>❌</td><td>❌</td><td>Never leaves <code>queryPmComparison</code>&#39;s dollar fields for a Manager: <code>attachPmProjectLer</code> (<code>lib/console-routes.js</code>) builds <code>req.console.user.pm_projects</code> carrying <code>{ id, name, ler }</code> only — the ratio, never <code>ler_revenue</code>/<code>ler_cost</code>. <code>/console/reports</code> (where the dollar components are rendered) is entirely <code>isOwnerLevel(user.role)</code>-gated at the top of the route.</td></tr><tr><td>LER <strong>ratio</strong></td><td>Sidebar &quot;My projects&quot; nav, project detail header pill</td><td>✅</td><td>✅</td><td>✅ <em>(only for projects they PM)</em></td><td>❌</td><td><code>pm_projects</code> is built in <code>requireConsoleSession</code> (<code>lib/console-routes.js</code>), scoped to <code>p.pm_user_id === user.id</code>, then enriched with <code>.ler</code> via <code>attachPmProjectLer</code>/<code>queryPmComparison</code> (issue #2092). A Manager who PMs nothing gets an empty array and the pill never renders anywhere for them.</td></tr><tr><td><code>agreed_amount</code> (top-level Liabilities overview)</td><td>Top-level Liabilities overview (<code>/console/liabilities</code>)</td><td>✅</td><td>✅</td><td>✅ <em>(only for liabilities on projects they PM)</em></td><td>❌</td><td><code>checkLiabilityReadPermission</code> (<code>liabilities.js</code>) gates the whole <code>list_liabilities</code> endpoint to owner+manager; the handler then filters a Manager&#39;s results to <code>managedProjectIds</code> (projects where <code>pm_user_id === </code> the requester).</td></tr><tr><td><code>agreed_amount</code> (project detail Liabilities card)</td><td>Project detail Liabilities card (<code>/console/p/:id</code>)</td><td>✅</td><td>✅</td><td>❌ <em>(no PM exception on this surface)</em></td><td>❌</td><td><code>vm.liabilities</code> on <code>/p/:projectId</code> is set only <code>if (isOwnerLevel(user.role))</code> (<code>lib/console-routes.js</code>) — unlike the top-level list above, there is <strong>no</strong> PM-of-project exception here: a Manager who PMs the project still sees no Liabilities card on its own detail page. Verified directly: <code>tests/console-liabilities.test.js</code> uses a Manager fixture (<code>Bob</code>) who <em>is</em> PM of the project under test and still asserts &quot;no liabilities card at all.&quot;</td></tr><tr><td>Salary bands, <code>discipline_levels</code>, <code>employer_cost_multiplier</code>, <code>users.level</code></td><td>Bands tab and Direct labor tab of <code>/console/users</code></td><td>✅</td><td>✅</td><td>❌</td><td>❌</td><td>Whole-route gate: <code>isOwnerLevel(user.role)</code> at the top of <code>router.get(&#39;/users&#39;, ...)</code> (<code>lib/console-routes.js</code>) covers every tab, so a Manager never reaches the tab-dispatch logic at all. The underlying MCP endpoints (<code>list_discipline_level_bands</code>, <code>set_discipline_level_band</code>, <code>set_employer_cost_multiplier</code>, <code>mcp-server.js</code>) are console-only (absent from <code>MCP_TOOLS</code>) and independently owner-gated.</td></tr><tr><td>Revenue / cost / margin / profit figures</td><td>Monthly P&amp;L, Forecast, project detail Financials sub-panels (Envelope budget, Accrued P&amp;L, Forecasted P&amp;L), Segmentation</td><td>✅</td><td>✅</td><td>❌</td><td>❌</td><td><code>/console/reports</code> and <code>/console/forecast</code> are whole-route <code>isOwnerLevel</code>-gated. On <code>/console/p/:id</code>, <code>vm.labor_budget</code> / <code>vm.forecast_pnl</code> / the Accrued-P&amp;L fields on <code>vm.analytics</code> are all set only inside the same <code>if (isOwnerLevel(user.role))</code> block as <code>contract_value</code> (<code>lib/console-routes.js</code>).</td></tr><tr><td>Deadlines</td><td>Project detail config card, capacity burn-pace context</td><td>✅</td><td>✅</td><td>✅</td><td>❌</td><td><code>vm.deadline</code> is set <code>if (user.role !== &#39;user&#39;)</code> in the <code>/p/:projectId</code> handler (<code>lib/console-routes.js</code>). At the MCP layer, <code>get_project_info</code> (<code>mcp-server.js</code>) adds <code>response.deadline</code> only <code>if (canSeeDeadline(requestingRole))</code>, where <code>canSeeDeadline</code> returns true for owner, admin, and manager (never <code>user</code>).</td></tr><tr><td>Hours, capacity, envelope figures</td><td><code>/console/capacity</code>, <code>/console/time</code>, envelope tables everywhere</td><td>✅</td><td>✅</td><td>✅</td><td>✅ <em>(own hours only)</em></td><td>No dollar-denominated field is ever included in <code>queryCapacityWeeks</code> / <code>computeAvailabilityForUser</code> (<code>lib/availability.js</code>) — visibility is scoped by <strong>whose</strong> hours are shown, not gated by role. A <code>user</code> sees only their own; every other role sees the whole team.</td></tr></tbody></table><hr><h2 id="reviewing-a-change-against-this-contract" tabindex="-1">Reviewing a change against this contract <a class="header-anchor" href="#reviewing-a-change-against-this-contract" aria-label="Permalink to &quot;Reviewing a change against this contract&quot;">​</a></h2><p>Before merging a change that touches a route, endpoint, or renderer carrying any field in the table above, ask:</p><ol><li><strong>Does this add a field to a payload a lower role can reach?</strong> Trace the object that reaches the renderer (or the JSON a route returns) all the way from the route handler — not just the final <code>&lt;dl&gt;</code> or table row that renders it.</li><li><strong>Is it gated in the route, or only in the renderer?</strong> A route-level omission (the field is never <em>set</em> on the object for that role) is the strong form — a renderer-level <code>if (isOwner)</code> around the markup is a second, weaker layer that a later refactor can silently drop. Prefer the route-level gate; treat a renderer-only gate as a defect waiting to happen, not a substitute.</li><li><strong>Is the field absent, or merely <code>null</code>/unrendered?</strong> <code>&#39;hourly_cost&#39; in payload === false</code> is a different, stronger claim than <code>payload.hourly_cost === null</code> or &quot;the HTML doesn&#39;t currently show it.&quot; #2095 shipped specifically to make the first claim true, not the second — see the row above for why.</li><li><strong>Does the new surface already have a row in this table?</strong> If not, add one — naming the real enforcement point — in the same PR, per the Documentation Sync Protocol in <code>CLAUDE.md</code> (repo root — not part of this published site, so deliberately not linked).</li></ol><h3 id="a-note-on-mutation-1-and-assert-on-the-response-body" tabindex="-1">A note on Mutation 1 and &quot;assert on the response body&quot; <a class="header-anchor" href="#a-note-on-mutation-1-and-assert-on-the-response-body" aria-label="Permalink to &quot;A note on Mutation 1 and &quot;assert on the response body&quot;&quot;">​</a></h3><p>Most rows above are enforced by a route-level field omission feeding a <strong>presence-gated</strong> renderer (<code>if (project.contract_value !== undefined)</code>) — for those, a regression that reintroduces the field into a lower role&#39;s payload shows up directly in the rendered response body, and <code>tests/data-visibility.test.js</code> checks it there.</p><p><code>hourly_cost</code> on <code>/console/people/:userId</code> is the one exception, and it is double-gated: both the route (which field it <em>sets</em>) and the renderer (<code>isOwnerLevel(user.role)</code>, not field presence) key off the requester&#39;s role independently. A route-level regression there — reintroducing <code>person.hourly_cost</code> for a Manager — would not change a single byte of the rendered HTML, because the renderer&#39;s own gate is unconditional on role and never looks at whether the field is present. Catching that specific regression requires inspecting the object handed to the renderer, not just the HTML it produces — the same technique <code>tests/console-person.test.js</code> already uses for its own #2095 &quot;leak test.&quot; <code>tests/data-visibility.test.js</code> uses it too, for this one field only, and says why inline.</p>',15)])])}const g=t(a,[["render",s]]);export{u as __pageData,g as default};
