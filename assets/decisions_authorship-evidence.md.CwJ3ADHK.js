import{_ as a,o as t,c as o,ag as s}from"./chunks/framework.8tqBsFWt.js";const u=JSON.parse('{"title":"Authorship Evidence — Human Creative Direction Record","description":"","frontmatter":{},"headers":[],"relativePath":"decisions/authorship-evidence.md","filePath":"decisions/authorship-evidence.md"}'),n={name:"decisions/authorship-evidence.md"};function r(i,e,d,c,l,h){return t(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="authorship-evidence-—-human-creative-direction-record" tabindex="-1">Authorship Evidence — Human Creative Direction Record <a class="header-anchor" href="#authorship-evidence-—-human-creative-direction-record" aria-label="Permalink to &quot;Authorship Evidence — Human Creative Direction Record&quot;">​</a></h1><p><strong>Issue:</strong> <a href="https://github.com/talktalkmake/VERA/issues/551" target="_blank" rel="noreferrer">#551</a> · <strong>Date:</strong> 2026-05-03 · <strong>Status:</strong> Canonical</p><hr><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p>This document preserves a record of meaningful human creative decisions made throughout VERA&#39;s development. It exists because copyright protection for AI-assisted code requires evidence that a human author made genuine creative choices — choosing architecture, deciding what to reject, restructuring output.</p><p>Directing an AI model toward an objective is not sufficient. Directing <em>how</em> the work is constructed is. This file documents the latter.</p><hr><h2 id="foundational-architectural-decisions" tabindex="-1">Foundational Architectural Decisions <a class="header-anchor" href="#foundational-architectural-decisions" aria-label="Permalink to &quot;Foundational Architectural Decisions&quot;">​</a></h2><h3 id="multi-tenant-model-via-asynclocalstorage-not-request-context-threading" tabindex="-1">Multi-tenant model via AsyncLocalStorage (not request context threading) <a class="header-anchor" href="#multi-tenant-model-via-asynclocalstorage-not-request-context-threading" aria-label="Permalink to &quot;Multi-tenant model via AsyncLocalStorage (not request context threading)&quot;">​</a></h3><p>Decided to carry tenant context through the entire async call chain using <code>AsyncLocalStorage</code> rather than threading a <code>tenant</code> parameter through every function signature. This decision was made after rejecting the parameter-threading approach as brittle — a single missing parameter in a deeply nested call silently reads from the wrong tenant. The ALS approach makes context implicit and fail-closed: if no tenant context is set, the call throws rather than silently serving another tenant&#39;s data.</p><h3 id="two-spreadsheet-architecture-database-sheet-reporting-sheet" tabindex="-1">Two-spreadsheet architecture (database sheet + reporting sheet) <a class="header-anchor" href="#two-spreadsheet-architecture-database-sheet-reporting-sheet" aria-label="Permalink to &quot;Two-spreadsheet architecture (database sheet + reporting sheet)&quot;">​</a></h3><p>Decided to separate the database Google Sheet (owned exclusively by the service account) from the reporting Google Sheet (owned by the agency, shared with clients via <code>IMPORTRANGE</code>). This was a deliberate rejection of a single-sheet design where the bot and clients would both read from the same file. The separation means the service account never needs access to the client-visible file, and the bot never has to sanitise its own data for client visibility.</p><h3 id="positional-column-reading-index-based-not-header-name-based" tabindex="-1">Positional column reading (index-based, not header-name-based) <a class="header-anchor" href="#positional-column-reading-index-based-not-header-name-based" aria-label="Permalink to &quot;Positional column reading (index-based, not header-name-based)&quot;">​</a></h3><p>Decided to read every Google Sheets tab by column position rather than by header name. This was a deliberate choice to avoid runtime header parsing, which would make the bot sensitive to minor label changes in a spreadsheet it cannot control. The trade-off is a strict schema contract documented in <code>CLAUDE.md</code> — any column reordering silently breaks reads. The contract is enforced by test fixtures, not by runtime header detection.</p><h3 id="write-ahead-log-wal-for-sheets-propagation-delay" tabindex="-1">Write-ahead log (WAL) for Sheets propagation delay <a class="header-anchor" href="#write-ahead-log-wal-for-sheets-propagation-delay" aria-label="Permalink to &quot;Write-ahead log (WAL) for Sheets propagation delay&quot;">​</a></h3><p>After observing Google Sheets&#39; server-side read/write propagation delay causing a newly appended row to be invisible on the immediately subsequent read, the decision was made to implement a client-side WAL in <code>lib/dal.js</code> rather than retry-looping reads or sleeping after writes. The WAL approach was chosen over a sleep-then-read retry because the propagation delay is non-deterministic — a sleep that works today may fail tomorrow. Three tables are covered: <code>07. Budgets</code>, <code>09. Conversations</code>, <code>01. Time Entries</code>.</p><h3 id="dal-dispatcher-pattern-no-raw-sheets-writes-in-callers" tabindex="-1">DAL dispatcher pattern (no raw Sheets writes in callers) <a class="header-anchor" href="#dal-dispatcher-pattern-no-raw-sheets-writes-in-callers" aria-label="Permalink to &quot;DAL dispatcher pattern (no raw Sheets writes in callers)&quot;">​</a></h3><p>Decided to route all reads and writes through a single dispatcher in <code>lib/dal.js</code> rather than calling <code>googleapis.spreadsheets.values.append</code> directly from business logic. This decision was made when Postgres shadow writes were introduced — without the dispatcher, adding a second write target to 47 call sites would have been individually error-prone. The invariant (&quot;no raw Sheets writes outside <code>lib/dal.sheets.js</code>&quot;) was enforced retroactively across five clusters of cleanup PRs (#483–#487) before any Postgres read flips.</p><h3 id="postgres-migration-strategy-fail-closed-env-gate-not-code-gate" tabindex="-1">Postgres migration strategy (fail-closed env gate, not code gate) <a class="header-anchor" href="#postgres-migration-strategy-fail-closed-env-gate-not-code-gate" aria-label="Permalink to &quot;Postgres migration strategy (fail-closed env gate, not code gate)&quot;">​</a></h3><p>Decided to gate Postgres writes on <code>PG_SHADOW_WRITE_TENANTS</code> — an env var listing tenant IDs — rather than on a per-tenant database column or code flag. This means adding a tenant to Postgres is a deploy-time operation, not a runtime one, and an env var that is absent (or empty) means zero Postgres writes anywhere. The fail-closed default was chosen explicitly: a misconfigured deploy is better discovered as a missing write than as an undetected dual-write inconsistency.</p><h3 id="permission-model-three-tiers-owner-manager-user" tabindex="-1">Permission model (three tiers: Owner / Manager / User) <a class="header-anchor" href="#permission-model-three-tiers-owner-manager-user" aria-label="Permalink to &quot;Permission model (three tiers: Owner / Manager / User)&quot;">​</a></h3><p>Decided to implement three permission tiers rather than a flat or RBAC-style model. The tiers were designed around what each person in an agency actually needs to know: Owners see dollars and margins; Managers see percentages and their assigned projects; Users see only their own time entries. The decision to hide deadlines entirely from Users was made after considering that surfacing project timelines to individual contributors creates pressure without context — managers see deadlines, users see allocation envelopes.</p><h3 id="allocation-envelope-model-not-project-membership" tabindex="-1">Allocation envelope model (not project membership) <a class="header-anchor" href="#allocation-envelope-model-not-project-membership" aria-label="Permalink to &quot;Allocation envelope model (not project membership)&quot;">​</a></h3><p>Decided that project &quot;assignment&quot; means having an active hour envelope in <code>07. Budgets</code>, not membership in a separate project-users table. This removes a whole class of sync problem: there is no state to get out of sync between &quot;is this person on the project?&quot; and &quot;does this person have hours to spend?&quot;. The envelope <em>is</em> the assignment. Anyone can log time to any active project — the envelope is for planning visibility, not access control.</p><h3 id="vendor-users-vs-contractor-users" tabindex="-1">Vendor users vs contractor users <a class="header-anchor" href="#vendor-users-vs-contractor-users" aria-label="Permalink to &quot;Vendor users vs contractor users&quot;">​</a></h3><p>Decided to model the fte/contractor/vendor distinction on <code>employment_type</code> rather than a separate <code>is_vendor</code> boolean or a separate users table. The three values encode: does this person log their own time (fte/contractor), or does a PM log for them (vendor)? Do they receive allocation envelopes (fte/contractor), or are they tracked as a fixed cost liability (vendor)? This was a deliberate rejection of a separate vendors table that would have duplicated the user schema.</p><h3 id="slug-based-dashboard-authentication-over-token-query-param" tabindex="-1">Slug-based dashboard authentication (over token + query param) <a class="header-anchor" href="#slug-based-dashboard-authentication-over-token-query-param" aria-label="Permalink to &quot;Slug-based dashboard authentication (over token + query param)&quot;">​</a></h3><p>Decided to introduce a <code>/d/:slug</code> vanity URL as the recommended dashboard auth mechanism instead of the original <code>?token=</code> query parameter. The slug serves as both the route and the credential — there is nothing else to share. The decision to use <code>crypto.timingSafeEqual</code> for comparison was made because timing attacks on token comparison are real and easy to avoid.</p><hr><h2 id="rejected-approaches-documented-as-evidence-of-design-choice" tabindex="-1">Rejected Approaches (Documented as Evidence of Design Choice) <a class="header-anchor" href="#rejected-approaches-documented-as-evidence-of-design-choice" aria-label="Permalink to &quot;Rejected Approaches (Documented as Evidence of Design Choice)&quot;">​</a></h2><p>The following approaches were explicitly evaluated and rejected. Rejection decisions represent meaningful human creative input.</p><table tabindex="0"><thead><tr><th>Approach</th><th>Rejected in favour of</th><th>Reason</th></tr></thead><tbody><tr><td>Header-name column reading in Sheets</td><td>Positional index reading</td><td>Fragile to client spreadsheet edits</td></tr><tr><td>Retry-loop after Sheets write</td><td>Client-side WAL</td><td>Non-deterministic propagation delay</td></tr><tr><td>Single <code>tenants</code> table in Postgres</td><td>Per-tenant JSON config file</td><td>Simpler ops; no DB needed to boot</td></tr><tr><td>Weekly schedules for allocations</td><td>Project-lifetime envelopes</td><td>Apples-to-oranges availability comparison</td></tr><tr><td>Separate vendors table</td><td><code>employment_type</code> on users</td><td>Avoids dual-schema maintenance</td></tr><tr><td>project_users membership table</td><td>Envelope-as-assignment model</td><td>Eliminates sync between membership and hours</td></tr><tr><td>AGPL or MIT open-source licence</td><td>Proprietary all-rights-reserved</td><td>Single-author product, no contributor community</td></tr></tbody></table><hr><h2 id="commit-message-standard-human-authorship-evidence" tabindex="-1">Commit Message Standard (Human Authorship Evidence) <a class="header-anchor" href="#commit-message-standard-human-authorship-evidence" aria-label="Permalink to &quot;Commit Message Standard (Human Authorship Evidence)&quot;">​</a></h2><p>To maintain a clear record of human creative direction, commits that implement AI-assisted code should follow this format:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;verb&gt; &lt;what changed&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Decision: &lt;why this approach was chosen over alternatives&gt;</span></span>
<span class="line"><span>Rejected: &lt;what was considered and not used&gt;</span></span></code></pre></div><p>Example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>route all Sheets writes through DAL dispatcher</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Decision: centralising all append/update calls in lib/dal.sheets.js</span></span>
<span class="line"><span>ensures pg shadow writes fire on every path without per-call changes.</span></span>
<span class="line"><span>Rejected: adding pg calls individually at each of 47 call sites — too</span></span>
<span class="line"><span>error-prone and would have missed sites silently.</span></span></code></pre></div><p>When a commit is a straightforward mechanical change with no architectural content (e.g. fixing a typo, bumping a version), a plain one-line message is fine.</p>`,39)])])}const m=a(n,[["render",r]]);export{u as __pageData,m as default};
